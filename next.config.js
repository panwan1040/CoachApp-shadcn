/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            "dicebear.com",
            "api.dicebear.com",
            "utfs.io",
            
        ]
    }
}

module.exports = nextConfig
